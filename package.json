{
  "name": "mova-flow-meet-recorder",
  "version": "0.1.0",
  "description": "Chrome extension that records Google Meet calls (tab audio + microphone) and transcribes them via a local Mova Flow server.",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.290",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
